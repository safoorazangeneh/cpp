#include<iostream>
#include<conio.h>
using namespace std;
main(){
	char ch;
	cout<<"enter a character:";
	cin>>ch;
	if(ch=='a'|| ch=='A')
		cout<<"yor want amber";
	if(ch=='b')
		cout<<"yor want black";
	if(ch=='B')
		cout<<"yor want blue";
	if(ch=='c')
		cout<<"yor want cream";
	if(ch=='C')
		cout<<"yor want copper";
	if(ch=='g')
		cout<<"yor want gray";
	if(ch=='G')
		cout<<"yor want green";
	if(ch=='m'|| ch=='M')
		cout<<"yor want magenta";
	if(ch=='n'|| ch=='N')
		cout<<"yor want navy";
	if(ch=='m'|| ch=='O')
		cout<<"yor want olive";
	if(ch=='m'|| ch=='P')
		cout<<"yor want pink";
	if(ch=='m'|| ch=='R')
		cout<<"yor want red";
	if(ch=='m'|| ch=='S')
		cout<<"yor want silver";
	if(ch=='m'|| ch=='T')
		cout<<"yor want teal";
	if(ch=='m'|| ch=='V')
		cout<<"yor want violet";
	if(ch=='m'|| ch=='W')
		cout<<"yor want white";
	if(ch=='m'|| ch=='Y')
		cout<<"yor want yellow";
	else
		cout<<"there are no color for this character";
			getch();
		
		
		
	
		
}
